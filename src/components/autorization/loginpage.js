import React, { Component } from 'react';

import Login from '../login/field_login';

const LoginPage = () => {
      return (
        <div>
          <Login />
        </div>
      );
    }
    
    
    export default LoginPage;